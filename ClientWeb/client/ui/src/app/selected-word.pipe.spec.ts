import { SelectedWordPipe } from './selected-word.pipe';

describe('SelectedWordPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectedWordPipe();
    expect(pipe).toBeTruthy();
  });
});
